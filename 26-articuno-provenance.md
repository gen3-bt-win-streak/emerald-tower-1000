# 26. フリーザー（XD産）の来歴証明ドシエ【2026-09-08・草稿】

> **対象個体（正）**：Pokémon XD シャドウフリーザー（PKHeX "XD Shadow Encounter 76"・シトンダーク島・Lv50・リライブ技 じんつうりき／いやしのすず／くろいきり／れいとうビーム）
> おだやか・IV **31/0/31/29/31/31**（H/A/B/C/D/S）・PID **`2F3C902C`**・XDRNG origin seed **`ECFE3E26`**・PSV 6114・通常色。PKHeX Legal（PIDType CXD）は確認済み。
> 本章の目的は「PKHeX Legalだから」ではなく、**XDの乱数アルゴリズムから独立に生成可能であること**と、**実機での入手ルート**、**Smogon記録申請時に何を証拠として出すか**を確定すること。
> 再現スクリプト：`tools/remote-audit/xd_articuno_verify.py`（PokeFinderのテストベクタ `pf_nonlock_vectors.json` を同梱）。

## 0. 結論（先に）

1. **XD RNG上で生成可能**。origin seed `ECFE3E26` からXDRNG（乗数0x343FD・加数0x269EC3）を5フレーム進めるとIV1/IV2/特性/PID上位/PID下位がそのまま対象個体になる。PKHeXの逆算ルーチンとは別に、**PokeFinderの生成ルーチンを移植して同社のテストベクタ50件を全一致**させた実装で導出・逆算・ロック連鎖を確認した（§2・§3）。
2. **「おだやか 31/0/31/31/31/31」はXDでは生成不可能**（直接経路にも、アンチシャイニー再抽選の全深度にも存在しない）。A0/C31を出す乱数状態は32bit空間に**4つしかなく**、性格は直接でてれい／がんばりや／しんちょう／きまぐれ、再抽選1回でようき／ずぶとい／わんぱく／おくびょう（いずれも特定のTID/SIDが必要）。**おだやかはC29が上限**（§4）。
3. 証拠の保存は§8のチェックリスト。Smogonルールと記録者の公開状況は§6（ワークフロー結果を反映後に確定）。

## 1. XDシャドウ生成の仕組み

（ワークフローの検証結果を反映して確定する。以下は当方がソースで直接確認した範囲）

- **XDRNG**：`seed' = seed × 0x343FD + 0x269EC3 (mod 2^32)`、出力は上位16bit。逆行は `× 0xB9B33155 + 0xA170F641`。PokeFinder `Core/RNG/LCRNG.hpp`（`using XDRNG = LCRNG<0x269EC3, 0x343FD>`）、PKHeX `XDRNG.cs`（Mult/Add/rMult/rAdd）で一致。
- **非ロック個体の生成順**（PokeFinder `GameCubeGenerator::generateNonLock`）：IV1（HP|A<<5|B<<10）→ IV2（S|C<<5|D<<10）→ 特性（1bit、特性が1つの種は0固定）→ PID上位 → PID下位 →**（hi ^ lo ^ プレイヤーTSV) < 8 なら PIDペアを再抽選**（アンチシャイニー）。
- **PKHeXの逆算**（`MethodFinder.GetXDRNGMatch`）：PIDペアを出す状態から2つ戻った2フレームがIV2/IV1に一致すれば `CXD`、途中に色違いペアが挟まれば `CXDAnti`。PKHeXはさらに `TeamLockResult` でグリーンビルの先行シャドウ4体（サイドン／ファイヤー／ナッシー／ケンタロス、5通りのSeen変種）を逆順に再生成し、CPUトレーナーのTID/SIDフレームまで辿る。フリーザーの先行4体は性格ロックなし（`NPCLock.MatchesLock`＝`Shadow && Nature==0`で常に真）。
- **PokeFinderの位置づけ**：シャドウロック一覧（77件）に3鳥は含まれず、**フリーザーは「Non Shadow Locks」静的リストのindex 67・Shiny::Never**として上記の非ロック経路で生成・検索される（EncounterTableGenerator サブモジュール 2026-02-28 "Correct Gales encounters" 以降のmaster。配布バイナリ4.3.0に含まれるかは未確認＝§9）。

## 2. 対象個体の導出（独立実装）

| フレーム | 状態 | 上位16bit | 意味 |
|---|---|---|---|
| origin | `ECFE3E26` | — | IV1直前の状態 |
| s1 | `7C1FFC51` | `7C1F` | IV1：HP31／A0／B31 |
| s2 | `FFBF2DD0` | `FFBF`→`7FBF` | IV2：S31／C29／D31 |
| s3 | `52845553` | `5284` | 特性bit 0（プレッシャー1つなので無関係） |
| s4 | `2F3CAACA` | `2F3C` | PID上位 |
| s5 | `902C4665` | `902C` | PID下位 |

PID `2F3C902C` → mod 25 = 20 ＝ **おだやか**、PSV = (0x2F3C ^ 0x902C) >> 3 = **6114**。プレイヤーの (TID ^ SID) >> 3 が6114でなければ再抽選は起きず、この個体がそのまま得られる。

**独立性の担保**：この実装は PokeFinder `generateNonLock` の移植で、PokeFinder同梱のテストベクタ（`Test/Gen3/gamecube.json` generateNonLock：Colo Umbreon／Espeon／Ageto Celebi／Gales Eevee／Gales Chikorita、各10状態＝50件）を**全一致**で通過する。PKHeXのコードは使っていない。

## 3. 逆算とロック連鎖

- **逆算（PKHeX `GetXDRNGMatch` の移植）**：PID `2F3C902C` と IV 31/0/31/29/31/31 から origin を求めると **`ECFE3E26` の1件のみ**。
- **ロック連鎖（PKHeX `TeamLockResult` の移植）**：5変種すべてで有効。

| 変種 | ケンタロス | ナッシー | ファイヤー | サイドン | CPU TID/SID |
|---|---|---|---|---|---|
| 全員未確認 | @0 `DD41F48A` | @7 `97EA6075` | @14 `82AD462B` | @21 `DB7D5915` | @28 `0300`/`8918` |
| サイドン・ファイヤー確認済 | @0 `DD41F48A` | @7 `97EA6075` | @14 `82AD462B` | @19 `0602A4C9` | @24 `5BD9`/`766D` |
| ＋ケンタロス確認済 | @0 `DD41F48A` | @5 `2D531C69` | @12 `D0DA1FC4` | @17 `53BBF146` | @22 `3C29`/`DB7D` |
| ＋ナッシー確認済 | @0 `DD41F48A` | @7 `97EA6075` | @12 `D0DA1FC4` | @17 `53BBF146` | @22 `3C29`/`DB7D` |
| 全員確認済 | @0 `DD41F48A` | @5 `2D531C69` | @10 `DAD61420` | @15 `F4F382AD` | @20 `5915`/`0602` |

（@n は origin の2フレーム前を0とする逆行フレーム。先行4体は性格ロックなしなので、PKHeXの検証はCPU側の色違い衝突が無いことだけを見る。5変種すべて衝突なし。）

## 4. 「おだやか 31/0/31/31/31/31」の厳密判定

HP31・A0・B31 を出すIV1と、S31・C31・D31 を出すIV2 が連続する乱数状態は **32bit空間に4つだけ**。それぞれのPID列（直接→再抽選1回→2回…）：

| origin | 直接 | 再抽選1回（要TSV） | 2回以降 |
|---|---|---|---|
| `2405CA1E` | てれい `7865E69D` | ようき `93EB2DCE`（TSV 5087） | 不可（PSV不一致） |
| `9243BC41` | うっかりや `1D8787EB` | **ずぶとい `6D1DDD5B`（TSV 4941）** | 不可 |
| `A405CA1E` | **しんちょう `F865669D`**（16章の予備個体） | わんぱく `13EBADCE`（TSV 5087） | 不可 |
| `1243BC41` | きまぐれ `9D8707EB` | **おくびょう `ED1D5D5B`（TSV 4941）** | 不可 |

→ **おだやか A0/C31 は、直接経路にも再抽選経路にも存在しない。** おだやかA0で得られるCは 29／28／26／17／16／15／4／3 の8通りが全て（§2の個体はその最大C29）。

「XD3鳥の理想個体」として流布している **ずぶとい 31/0/31/31/31/31／おくびょう 31/0/31/31/31/31** は上表の再抽選1回経路（origin `9243BC41`／`1243BC41`、**TID^SID>>3 = 4941 のセーブでのみ出現**。PKHeX issue #3008 の実例 TID 7／SID 39528 がまさにこれ）。**ひかえめ 31/4/31/31/31/31（`5A134534`）／おくびょう 31/6/31/31/31/31（`883EC465`）／おだやか 31/5/31/31/31/31（`1E98B426`）は直接経路**に存在する。つまり流布リストは「性格ごとの最良既知個体」であり、A0が不可能という主張ではない（出典はワークフロー結果を反映）。

## 5. 色違い不可の根拠 ／ 6. Smogonルールと記録者 ／ 7. 証明力 ／ 8. 保存チェックリスト ／ 9. 出典

（ワークフローの研究・検証結果を反映して記述する）
