# 03. Phase 3：AI解析（pokeemeraldコード解析・完了版）

> 出典はすべて pokeemerald 一次資料のコード行（battle_ai_scripts.s / battle_ai_script_commands.c /
> battle_ai_switch_items.c / battle_main.c 等）。「実測」ではなく「実装」なので、確率まで正確。

## 0. タワーの敵AIの正体【確定】

- タワーの全トレーナーのAIフラグは一律 **CHECK_BAD_MOVE｜TRY_TO_FAINT｜CHECK_VIABILITY**、
  ダブルでは **DOUBLE_BATTLE** フラグが自動追加（battle_ai_script_commands.c:371-379）。
  トレーナー個体差はない（＝**全員同じ思考ルーチン**。違いは手持ちセットだけ）。
- 技スコアは各技100からスタートし、スクリプトが加減点。**最高スコアの技を選び、同点はランダム**。
- フロンティアでは敵AIの**道具使用は無効**（持ち物データがAI履歴にロードされない）。

## 1. AIは何が「見えて」いるか【確定】

| 情報 | AIの扱い |
|---|---|
| こちらの技 | **使うまで知らない**（battleHistoryに使用済み技のみ記録） |
| こちらの特性・持ち物 | 建前は「公開情報のみ」だが… |
| ダメージ計算 | **実ステータス・実持ち物・実特性で計算する（実質チート）** |
| 素早さ・行動順 | 実数値で正確に判定（`if_user_goes`） |
| HP | 実数値を正確に参照 |
| 急所 | **一切考慮しない**（gCritMultiplier=1固定） |
| 命中率・連続技回数 | **一切考慮しない**（ふぶき・だいもんじを外れリスク無視で選ぶ） |
| ダメージ乱数 | ターンごとに技スロット単位で85〜100%を1回だけ疑似ロール → **「確定数」判定が実際とズレることがある** |

含意：
- **行動順・ダメージレンジはAIに正確に読まれている**前提で立ち回る（「調整で誤魔化す」は効かない）。
- 逆に**未使用の技は存在しないものとして扱われる**→ 初見の技（ほろびのうた等）への対策行動は取られない。

## 2. ターゲット選択（ダブル）【確定】

アルゴリズム（`ChooseMoveOrAction_Doubles`）:
1. 技×対象（敵2体＋味方＋自分）の全組み合わせを個別スコアリング
2. **その技で対象を倒せるなら+4**（先制技なら+6）→ これが「弱った獲物への集中攻撃」の正体（創発的。2体のAIが独立に同じ結論を出す）
3. 倒せない場合「自分の最大威力技でない技は−1」→ 基本は最大打点を押してくる
4. 対象間で最高スコアが同点なら**ランダムに対象選択**（差がなければ左右50/50）
5. 味方対象はスコア100未満なら除外（味方への地震誤爆は基本しないが、てだすけ/スキルスワップ等の連携は+加点で実行）

含意：
- **HPが削れた自軍ポケモンは正確に「倒せる」と判定され、2体から集中攻撃される**。
  ゲンガーの「生存優先（まもる）」は仕様的に正しい。逆に言えば、**HP満タン付近なら攻撃は分散しがち（50/50乱数）**。
- 敵の全体技（じしん等）の対象選択は固定なので読みやすい。

## 3. 技カテゴリ別のAI挙動【確定・スコア付き】

### まもる/みきり（敵の18セットが所持 → 01の§8）
- CHECK_VIABILITYのAIは**デフォルトでまもるに+2**（＝まもる持ちは初手まもる率がかなり高い）
- **連続使用カウンタが2以上なら−2**（＝3連続まもるはほぼ押さない。実機成功率も 100→50→25%）
- こちらに もうどく/のろい/やどりぎ/**ほろびのうた**/あくび が入っていると+2（スリップ稼ぎ）
- 自分（AI側）がスリップ状態だと加点なし
- **含意：爆発は敵の「まもる初回使用後」のターンに撃つと透かされにくい。**
  まもる持ち18セットの前では1ターン様子見→爆発が正解手順。

### 一撃必殺技
- レベル条件・がんじょう・無効タイプ以外の抑制なし。**特別に狙いもしない**（スコア素の100）
- 含意：一撃技持ちは**毎ターン約1/4〜1/2の確率で一撃技を押してくる**（他の技とのスコア並び次第）。
  「様子見してこなかったから安全」という読みは成立しない。即処理あるのみ。

### だいばくはつ/じばく（敵）
- 残HP≥80%（かつ自分が先手側）：約80%で−3 ＝ **ほぼ撃たない**
- 残HP51-79%：約80%で−1 ／ **残HP≤50%：50%で+1、≤30%はさらに+1 ＝ 高確率で撃つ**
- 対象がゴースト or 場にしめりけ：−10（撃たない）
- **自分が最後の1匹で相手に控えあり：−10（実質撃たない。※点数比較なので他技PP切れ時は選ばれ得る→12章§4.3）**
- **TRY_TO_FAINTとの相互作用（2026-08-11追記・原文確認）**：`AI_TryToFaint` は倒せる技に+4を与えるが
  **爆発は明示的に対象外**（`if_effect EFFECT_EXPLOSION, End`）。逆に誰も倒せない膠着では
  「最大打点でない技に−1」により最大打点の爆発が相対+1で浮上する。満タン低速勢（フォレトス等）の
  爆発が飛ぶ理由はこれ＋②の`if_target_faster`で−3が付かないことで完全に説明できる
- HPしきい値の正確な値＝**80/50/30の3段**（`hp < maxHP×N/100`の整数比較。80%ちょうどは≥80%側）。
  乱数は 206/256≈80.5% と 128/256=50%。回避ランク条件もあり（相手回避+1で−1、+4でさらに50%−1）
- ※AI_Risky（爆発+2）・AI_HPAware（HP帯抑制）にも爆発条項があるが、**タワーのAIフラグには両方無い＝動かない**
- 含意：**敵の爆発は「HPを半分以下に削った直後」に来る**。爆発持ち（01の§3）のHPを中途半端に削るのは危険。
  削るなら一撃で落とす、無理なら削る前にゲンガー以外を退避/まもる。

### でんじは・電磁波系
- **相手（こちら）が自分より速いと約92%で+3** ＝ 素早いゲンガー/ラティオスは電磁波の的
- 含意：電磁波持ちの前ではラム/カゴ管理を意識。まひ=素早さ1/4+行動不能25%は実質戦闘不能。

### 催眠技
- 戦術加点はバグでほぼ機能していないが、素の100で普通に選ばれる（4技の1つなら約1/4）
- あくびは減点対象にすらならない

### みちづれ
- 無条件−1、**自分が先手を取れて瀕死に近いほど加点**（HP≤30%で最大+3）
- 含意：敵ゲンガー等のみちづれは「削られた後の先手行動」で来る。倒すターンは「失っていい側」で殴る原則を維持

### ほろびのうた（敵）
- 特別な戦術評価なし（素の100）。ただし**こちらがほろび状態だと、敵はくろいまなざし系+1・まもる+2**
  ＝ ルージュラ#475/ムウマ#566の「歌→捕縛→まもる」コンボはAI仕様として実在する

### どくどく
- 自分がまもる系を持っていると約77%で+2 ＝ **どくどく+まもる型（スイクン#772等）はコンボを明確に狙ってくる**

## 4. 交代AI【確定・最重要】

敵が自発交代する経路は**6つだけ**（`ShouldSwitch`、優先順）：

| # | トリガー | 確率 | ダブルで有効? |
|---|---|---|---|
| 1 | **ほろびのうた**：カウント1表示の次ターン（＝倒れるターン）の行動選択時 | **100%（乱数なし）** | ✅ |
| 2 | 相手がふしぎなまもりで自分に抜群技なし、控えに抜群技持ちあり | 約2/3 | ❌ シングル限定 |
| 3 | 直前に受けた炎/水/電気技を吸収できる特性（もらいび/ちょすい/ちくでん）の控えがいる | 約1/2 | ✅ |
| 4 | 自分がねむり＋しぜんかいふく＋HP≥50% | 約1/2〜100% | ✅ |
| 5 | 直前に受けた技が控えに**無効**＋その控えが抜群技持ち（自分に抜群技なし等の前提付き） | 1/2 | ✅ |
| 6 | 同条件で**半減**の控え | 1/3 | ✅ |

共通の前提：交代封じ（くろいまなざし・かげふみ・まきつき系・ありじごく※・じりょく）を受けていない／生存控えがいる。
（※ありじごくは飛行/ふゆうにも効く判定バグあり）

**ほろびのうた交代の正確な仕様**：
- 交代は**倒れるはずの最終ターンの行動選択**で発生（それより早くは絶対逃げない）
- 交代先は「相性計算のバグで変な子が出てくる」ことも多い（最も不利なタイプの控え優先という有名バグ）
- 交代した瞬間ほろびカウントは消滅

**戦術への直結**：
1. **敵の残りが2匹になってから歌えば、交代先ゼロ＝3ターン後に2体同時確殺**（従来方針が正式に確定）
2. 敵に控えがいても、歌えば**最終ターンの敵の行動1つを交代で確定消費**させられる（実質テンポ+1）
3. くろいまなざしを当てれば控えがいても確殺（ゲンガーはLv13で習得可能）
4. 経路3/5/6の対策：**同じ技を無効/吸収系の控えに撃ち込み続けない**（水技→ちょすい系など）。
   爆発プランでは長期戦にならないため実害は小さい

## 5. その他の既知挙動【確定 or 高確度】

- ねむり中は「ねごと優先、**目覚めるターンにもねごとを選んで空振り**」（カゴ無しねむる型の目覚めターンは読める）
- じこあんじ持ちはこちらの積みをコピーしようとし続ける
- タイプ無効はAIが認識して撃たない（CHECK_BAD_MOVEで−10）。ただし**複合タイプの無効判定にバグ**があり、
  まれに無効技を撃ってくる（例：じめん技→ふゆう複合への誤射）＝こちらに有利な事故
- 1ターン目に状態異常技へ+2（約31%）する SetupFirstTurn フラグは**タワーAIには無い**（心配不要）

### 5.1 せんせいのツメの乱数は「場の全員で共有」【2026-08-11・実機の体感→原文で確定】

実戦報告「敵と自分のツメ発動がやたら重なる」は**気のせいではなく仕様**。`src/battle_main.c`：

```c
gRandomTurnNumber = Random();          // ← ターン開始時に1回だけ抽選（3923行・4013行）

// GetWhoStrikesFirst() 内、各バトラーの素早さ判定で同じ値を参照（4653行・4687行）
if (holdEffect == HOLD_EFFECT_QUICK_CLAW && gRandomTurnNumber < (0xFFFF * holdEffectParam) / 100)
    speedBattler = UINT_MAX;
```

- **ターン内の乱数は1個だけ**。場にいる全ツメ持ちが同じ `gRandomTurnNumber` を同じ閾値（20%＝13107/65536）と比較する
  → **発動するときは全員、しないときは全員**。確率的に独立ではなく**100%相関**する
- 両者が発動すると双方 `UINT_MAX` で**同値**になり、`if (speedBattler1 == speedBattler2 && Random() & 1)` の
  同速分岐へ落ちる＝**50/50のコイントス**。遅いグロス（実速177・310/546に抜かれる）にとっては
  「確実な後手」が「五分」に改善されるので、ツメ同士のかち合いは損ではない
- **ダブルでは4体全員が同じ値を見る**ため、ターンの流れが一気に反転して見えるのはこの共有が原因
- 実戦での逆用：**敵のツメ発動を目撃したターンは、こちらのツメも発動している**（同値・同閾値のため）。
  逆に敵が発動しなかったならこちらも不発。爆発を撃つターンの行動順読みに使える場面がある
- ※きあいのハチマキ（`HOLD_EFFECT_FOCUS_BAND`）は別枠の判定で、この共有とは無関係

## 6. 残る実機検証項目（優先度低）

- [ ] みちづれ状態の敵を爆発で倒した際の判定順（引き分け仕様と絡む）
- [ ] まもる+2デフォルトの実戦頻度感（コード確定値のプレイ感覚への翻訳）
- [ ] ありじごく判定バグの実害有無（定常プールのダグトリオ4セットと対面時）
